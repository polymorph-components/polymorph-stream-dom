// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// source: stream-dom-events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "polymorph.stream_dom";

/**
 * MouseEvent.button. Note the order differs from the `buttons` bits:
 * auxiliary (middle) is 1 here and the 4 bit there. Proto3 enums are open,
 * so a device's higher-numbered buttons arrive as their raw code.
 */
export enum MouseButton {
  PRIMARY = 0,
  AUXILIARY = 1,
  SECONDARY = 2,
  BACK = 3,
  FORWARD = 4,
  UNRECOGNIZED = -1,
}

/** WheelEvent.deltaMode: the unit of the deltas. */
export enum DeltaMode {
  PIXEL = 0,
  LINE = 1,
  PAGE = 2,
  UNRECOGNIZED = -1,
}

/** KeyboardEvent.location. */
export enum KeyLocation {
  STANDARD = 0,
  LEFT = 1,
  RIGHT = 2,
  NUMPAD = 3,
  UNRECOGNIZED = -1,
}

/**
 * A textarea or text-like input's live value and selection. This is optional
 * top-level event context rather than a family: input events still need their
 * FormData, while select/selectionchange events have no other payload.
 */
export enum SelectionDirection {
  SELECTION_DIRECTION_NONE = 0,
  SELECTION_DIRECTION_FORWARD = 1,
  SELECTION_DIRECTION_BACKWARD = 2,
  UNRECOGNIZED = -1,
}

/**
 * Keyboard modifiers held during the event: the DOM's altKey / ctrlKey /
 * metaKey / shiftKey. Absent means none held.
 */
export interface Modifiers {
  alt: boolean;
  ctrl: boolean;
  meta: boolean;
  shift: boolean;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** MouseEvent. Also the base of pointer, wheel and drag events. */
export interface MouseData {
  /** Viewport-relative (clientX/Y). */
  clientX: number;
  clientY: number;
  /** Document-relative (pageX/Y). */
  pageX: number;
  pageY: number;
  /** Physical screen (screenX/Y). */
  screenX: number;
  screenY: number;
  /** Relative to the target's padding edge (offsetX/Y). */
  offsetX: number;
  offsetY: number;
  /**
   * The button that changed state. Set only on events a press or release
   * caused (mousedown, mouseup, click, dblclick, auxclick, contextmenu and
   * their pointer / drag counterparts); the DOM reports a meaningless 0 on
   * mousemove and the enter / leave / over / out family, so it is absent
   * there rather than misreported as PRIMARY.
   */
  button?:
    | MouseButton
    | undefined;
  /** Buttons currently held (the bits of MouseEvent.buttons). */
  primary: boolean;
  secondary: boolean;
  auxiliary: boolean;
  back: boolean;
  forward: boolean;
  modifiers:
    | Modifiers
    | undefined;
  /**
   * relatedTarget as a node id (mouseover / out, mouseenter / leave);
   * absent when there is none or it is outside the mount.
   */
  relatedTarget?: number | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** PointerEvent: a MouseEvent plus the device geometry. */
export interface PointerData {
  mouse: MouseData | undefined;
  pointerId: number;
  /** Contact geometry in CSS pixels (width / height); 1x1 for a mouse. */
  width: number;
  height: number;
  /** 0..1; 0.5 for hardware without pressure while a button is held. */
  pressure: number;
  /** -1..1. */
  tangentialPressure: number;
  /** Degrees, -90..90. */
  tiltX: number;
  tiltY: number;
  /** Degrees, 0..359. */
  twist: number;
  /**
   * "mouse", "pen", "touch", "" when unknown; the DOM allows further
   * vendor-prefixed values, so this is not an enum.
   */
  pointerType: string;
  isPrimary: boolean;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** WheelEvent: a MouseEvent plus the scroll deltas. */
export interface WheelData {
  mouse: MouseData | undefined;
  deltaX: number;
  deltaY: number;
  deltaZ: number;
  deltaMode: DeltaMode;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** KeyboardEvent. */
export interface KeyboardData {
  /** The logical key ("a", "Enter", "ArrowLeft"); layout-dependent. */
  key: string;
  /** The physical key ("KeyA", "Enter"); layout-independent. */
  code: string;
  location: KeyLocation;
  repeat: boolean;
  /** The key was part of an IME composition. */
  isComposing: boolean;
  modifiers: Modifiers | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * One form entry. The whole form's entries are a list, not a map: order
 * matters and names repeat (checkbox groups, multi-select).
 */
export interface FormField {
  name: string;
  value: string;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * input / change / submit and kin. Files chosen in an `<input type=file>`
 * are not here; they are a WIT resource.
 */
export interface FormData {
  /**
   * The target control's current value (input, textarea, select); "" when
   * the target has none.
   */
  value: string;
  /** Set for checkable controls (checkbox, radio). */
  checked?:
    | boolean
    | undefined;
  /**
   * The enclosing form's entries (FormData(form).entries()), populated on
   * submit; empty otherwise. dioxus-web populates them on every event
   * inside a form, which serializes the whole form per keystroke — a
   * Dioxus adapter wanting that reads the controls it needs instead.
   */
  fields: FormField[];
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface TextControlData {
  value: string;
  /** DOM selectionStart/selectionEnd: UTF-16 code-unit offsets. */
  selectionStart: number;
  selectionEnd: number;
  direction: SelectionDirection;
  /**
   * True during an IME composition. A producer can defer controlled value
   * updates until a later event reports false.
   */
  isComposing: boolean;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * The target's scroll state at the time of a `scroll` event. The offsets
 * are fractional; the sizes are integer in the DOM.
 */
export interface ScrollData {
  scrollTop: number;
  scrollLeft: number;
  scrollWidth: number;
  scrollHeight: number;
  clientWidth: number;
  clientHeight: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** CompositionEvent (compositionstart / update / end). */
export interface CompositionData {
  /**
   * The text the IME contributed (CompositionEvent.data). "" is legitimate:
   * a composition can be cancelled to nothing.
   */
  data: string;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** AnimationEvent. */
export interface AnimationData {
  /** Which @keyframes animation fired (animationName). */
  animationName: string;
  /** The pseudo-element it ran on ("::before"), "" for the element itself. */
  pseudoElement: string;
  /**
   * Seconds the animation had run when the event fired, excluding paused
   * time.
   */
  elapsedTime: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** TransitionEvent. One `transitionend` fires per transitioned property. */
export interface TransitionData {
  propertyName: string;
  pseudoElement: string;
  elapsedTime: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** One finger: the DOM Touch object. */
export interface TouchPoint {
  /**
   * Stable for the life of one contact, so a handler can follow a finger
   * across touchmove events.
   */
  identifier: number;
  clientX: number;
  clientY: number;
  pageX: number;
  pageY: number;
  screenX: number;
  screenY: number;
  /**
   * Half-axes of the ellipse approximating the contact area; 0 on hardware
   * that does not report contact geometry.
   */
  radiusX: number;
  radiusY: number;
  /** Degrees, 0..90, that the contact ellipse is rotated by. */
  rotationAngle: number;
  /** Contact pressure, 0..1; 0 when unsupported. */
  force: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * TouchEvent. The three lists are the DOM's; a finger commonly appears in
 * more than one.
 */
export interface TouchData {
  /** Every finger currently on the surface (touches). */
  touches: TouchPoint[];
  /**
   * Only the fingers whose state this event reports (changedTouches) — on
   * touchend, the ones that lifted.
   */
  changedTouches: TouchPoint[];
  /** The subset of `touches` that started on the target (targetTouches). */
  targetTouches: TouchPoint[];
  modifiers: Modifiers | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * Geometry for the observer families. Structurally the same as the WIT
 * `queries` records: those are typed RPC results and these are wire
 * snapshots, and neither side can use the other's definition.
 */
export interface Size {
  width: number;
  height: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface Rect {
  x: number;
  y: number;
  width: number;
  height: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * Synthetic `resize`: one ResizeObserverEntry for the listening element.
 * The DOM reports both boxes writing-mode-relative (blockSize /
 * inlineSize); the receiver resolves that to width / height so the
 * producer need not know the element's writing mode.
 */
export interface ResizeData {
  /** Content plus padding plus border (borderBoxSize). */
  borderBox:
    | Size
    | undefined;
  /** The content box alone (contentBoxSize). */
  contentBox: Size | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * Synthetic `visible`: one IntersectionObserverEntry for the listening
 * element.
 */
export interface VisibleData {
  /** The target's own box at the time of observation (boundingClientRect). */
  boundingClientRect:
    | Rect
    | undefined;
  /** How much of the target is visible, 0..1 (intersectionRatio). */
  intersectionRatio: number;
  /** The visible part of the target (intersectionRect); all-zero when none. */
  intersectionRect:
    | Rect
    | undefined;
  /**
   * Whether the target intersects the root at all (isIntersecting).
   * Distinct from a nonzero ratio: an entry fires on the transition out of
   * view too.
   */
  isIntersecting: boolean;
  /**
   * The root's clipping box (rootBounds). Absent when the DOM reports null:
   * the root is the implicit viewport and the document is in a cross-origin
   * frame, so the browser withholds the parent's geometry.
   */
  rootBounds?:
    | Rect
    | undefined;
  /**
   * See the file header for the clock. The DOM's `time` is
   * document-relative; the receiver adds `performance.timeOrigin`.
   */
  timeMs: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * Synthetic `frame`: the receiver's requestAnimationFrame fired. Subscribed
 * on the mount root; tells an animating producer that the last commit
 * reached the screen and the next one is due.
 */
export interface FrameData {
  /** The frame's start time, on the same clock as VisibleData.time_ms. */
  timeMs: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/**
 * Families ordered roughly by frequency; all tags are one byte.
 * `popstate` and `hashchange` on `window`. The only reason to listen to
 * either is to learn where the document now is, and a producer has no
 * `location` to read, so the receiver snapshots it. (HashChangeEvent
 * carries newURL itself; PopStateEvent carries only `state`, so the
 * snapshot is uniform rather than per-event.)
 */
export interface NavigationData {
  /** location.href after the navigation. */
  href: string;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface EventPayload {
  family:
    | { $case: "mouse"; value: MouseData }
    | { $case: "keyboard"; value: KeyboardData }
    | { $case: "form"; value: FormData }
    | { $case: "pointer"; value: PointerData }
    | { $case: "wheel"; value: WheelData }
    | { $case: "touch"; value: TouchData }
    | { $case: "scroll"; value: ScrollData }
    | { $case: "frame"; value: FrameData }
    | { $case: "composition"; value: CompositionData }
    | { $case: "animation"; value: AnimationData }
    | { $case: "transition"; value: TransitionData }
    | { $case: "resize"; value: ResizeData }
    | { $case: "visible"; value: VisibleData }
    | { $case: "navigation"; value: NavigationData }
    | undefined;
  textControl?: TextControlData | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseModifiers(): Modifiers {
  return { alt: false, ctrl: false, meta: false, shift: false, _unknownFields: {} };
}

export const Modifiers: MessageFns<Modifiers> = {
  encode(message: Modifiers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alt !== false) {
      writer.uint32(8).bool(message.alt);
    }
    if (message.ctrl !== false) {
      writer.uint32(16).bool(message.ctrl);
    }
    if (message.meta !== false) {
      writer.uint32(24).bool(message.meta);
    }
    if (message.shift !== false) {
      writer.uint32(32).bool(message.shift);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Modifiers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseModifiers();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.alt = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.ctrl = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.meta = reader.bool();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.shift = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseMouseData(): MouseData {
  return {
    clientX: 0,
    clientY: 0,
    pageX: 0,
    pageY: 0,
    screenX: 0,
    screenY: 0,
    offsetX: 0,
    offsetY: 0,
    button: undefined,
    primary: false,
    secondary: false,
    auxiliary: false,
    back: false,
    forward: false,
    modifiers: undefined,
    relatedTarget: undefined,
    _unknownFields: {},
  };
}

export const MouseData: MessageFns<MouseData> = {
  encode(message: MouseData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientX !== 0) {
      writer.uint32(9).double(message.clientX);
    }
    if (message.clientY !== 0) {
      writer.uint32(17).double(message.clientY);
    }
    if (message.pageX !== 0) {
      writer.uint32(25).double(message.pageX);
    }
    if (message.pageY !== 0) {
      writer.uint32(33).double(message.pageY);
    }
    if (message.screenX !== 0) {
      writer.uint32(41).double(message.screenX);
    }
    if (message.screenY !== 0) {
      writer.uint32(49).double(message.screenY);
    }
    if (message.offsetX !== 0) {
      writer.uint32(57).double(message.offsetX);
    }
    if (message.offsetY !== 0) {
      writer.uint32(65).double(message.offsetY);
    }
    if (message.button !== undefined) {
      writer.uint32(72).int32(message.button);
    }
    if (message.primary !== false) {
      writer.uint32(80).bool(message.primary);
    }
    if (message.secondary !== false) {
      writer.uint32(88).bool(message.secondary);
    }
    if (message.auxiliary !== false) {
      writer.uint32(96).bool(message.auxiliary);
    }
    if (message.back !== false) {
      writer.uint32(104).bool(message.back);
    }
    if (message.forward !== false) {
      writer.uint32(112).bool(message.forward);
    }
    if (message.modifiers !== undefined) {
      Modifiers.encode(message.modifiers, writer.uint32(122).fork()).join();
    }
    if (message.relatedTarget !== undefined) {
      writer.uint32(128).uint32(message.relatedTarget);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MouseData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMouseData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.clientX = reader.double();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.clientY = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.pageX = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.pageY = reader.double();
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.screenX = reader.double();
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }

            message.screenY = reader.double();
            continue;
          }
          case 7: {
            if (tag !== 57) {
              break;
            }

            message.offsetX = reader.double();
            continue;
          }
          case 8: {
            if (tag !== 65) {
              break;
            }

            message.offsetY = reader.double();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.button = reader.int32() as any;
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.primary = reader.bool();
            continue;
          }
          case 11: {
            if (tag !== 88) {
              break;
            }

            message.secondary = reader.bool();
            continue;
          }
          case 12: {
            if (tag !== 96) {
              break;
            }

            message.auxiliary = reader.bool();
            continue;
          }
          case 13: {
            if (tag !== 104) {
              break;
            }

            message.back = reader.bool();
            continue;
          }
          case 14: {
            if (tag !== 112) {
              break;
            }

            message.forward = reader.bool();
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.modifiers = Modifiers.decode(reader, reader.uint32());
            continue;
          }
          case 16: {
            if (tag !== 128) {
              break;
            }

            message.relatedTarget = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBasePointerData(): PointerData {
  return {
    mouse: undefined,
    pointerId: 0,
    width: 0,
    height: 0,
    pressure: 0,
    tangentialPressure: 0,
    tiltX: 0,
    tiltY: 0,
    twist: 0,
    pointerType: "",
    isPrimary: false,
    _unknownFields: {},
  };
}

export const PointerData: MessageFns<PointerData> = {
  encode(message: PointerData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mouse !== undefined) {
      MouseData.encode(message.mouse, writer.uint32(10).fork()).join();
    }
    if (message.pointerId !== 0) {
      writer.uint32(16).sint32(message.pointerId);
    }
    if (message.width !== 0) {
      writer.uint32(25).double(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(33).double(message.height);
    }
    if (message.pressure !== 0) {
      writer.uint32(45).float(message.pressure);
    }
    if (message.tangentialPressure !== 0) {
      writer.uint32(53).float(message.tangentialPressure);
    }
    if (message.tiltX !== 0) {
      writer.uint32(56).sint32(message.tiltX);
    }
    if (message.tiltY !== 0) {
      writer.uint32(64).sint32(message.tiltY);
    }
    if (message.twist !== 0) {
      writer.uint32(72).sint32(message.twist);
    }
    if (message.pointerType !== "") {
      writer.uint32(82).string(message.pointerType);
    }
    if (message.isPrimary !== false) {
      writer.uint32(88).bool(message.isPrimary);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PointerData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePointerData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.mouse = MouseData.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.pointerId = reader.sint32();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.width = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.height = reader.double();
            continue;
          }
          case 5: {
            if (tag !== 45) {
              break;
            }

            message.pressure = reader.float();
            continue;
          }
          case 6: {
            if (tag !== 53) {
              break;
            }

            message.tangentialPressure = reader.float();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.tiltX = reader.sint32();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.tiltY = reader.sint32();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.twist = reader.sint32();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.pointerType = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 88) {
              break;
            }

            message.isPrimary = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseWheelData(): WheelData {
  return { mouse: undefined, deltaX: 0, deltaY: 0, deltaZ: 0, deltaMode: 0, _unknownFields: {} };
}

export const WheelData: MessageFns<WheelData> = {
  encode(message: WheelData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mouse !== undefined) {
      MouseData.encode(message.mouse, writer.uint32(10).fork()).join();
    }
    if (message.deltaX !== 0) {
      writer.uint32(17).double(message.deltaX);
    }
    if (message.deltaY !== 0) {
      writer.uint32(25).double(message.deltaY);
    }
    if (message.deltaZ !== 0) {
      writer.uint32(33).double(message.deltaZ);
    }
    if (message.deltaMode !== 0) {
      writer.uint32(40).int32(message.deltaMode);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WheelData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseWheelData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.mouse = MouseData.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.deltaX = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.deltaY = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.deltaZ = reader.double();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.deltaMode = reader.int32() as any;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseKeyboardData(): KeyboardData {
  return {
    key: "",
    code: "",
    location: 0,
    repeat: false,
    isComposing: false,
    modifiers: undefined,
    _unknownFields: {},
  };
}

export const KeyboardData: MessageFns<KeyboardData> = {
  encode(message: KeyboardData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.location !== 0) {
      writer.uint32(24).int32(message.location);
    }
    if (message.repeat !== false) {
      writer.uint32(32).bool(message.repeat);
    }
    if (message.isComposing !== false) {
      writer.uint32(40).bool(message.isComposing);
    }
    if (message.modifiers !== undefined) {
      Modifiers.encode(message.modifiers, writer.uint32(50).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyboardData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseKeyboardData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.code = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.location = reader.int32() as any;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.repeat = reader.bool();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.isComposing = reader.bool();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.modifiers = Modifiers.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseFormField(): FormField {
  return { name: "", value: "", _unknownFields: {} };
}

export const FormField: MessageFns<FormField> = {
  encode(message: FormField, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FormField {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFormField();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseFormData(): FormData {
  return { value: "", checked: undefined, fields: [], _unknownFields: {} };
}

export const FormData: MessageFns<FormData> = {
  encode(message: FormData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    if (message.checked !== undefined) {
      writer.uint32(16).bool(message.checked);
    }
    for (const v of message.fields) {
      FormField.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FormData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFormData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.value = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.checked = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.fields.push(FormField.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseTextControlData(): TextControlData {
  return { value: "", selectionStart: 0, selectionEnd: 0, direction: 0, isComposing: false, _unknownFields: {} };
}

export const TextControlData: MessageFns<TextControlData> = {
  encode(message: TextControlData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    if (message.selectionStart !== 0) {
      writer.uint32(16).uint32(message.selectionStart);
    }
    if (message.selectionEnd !== 0) {
      writer.uint32(24).uint32(message.selectionEnd);
    }
    if (message.direction !== 0) {
      writer.uint32(32).int32(message.direction);
    }
    if (message.isComposing !== false) {
      writer.uint32(40).bool(message.isComposing);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextControlData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTextControlData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.value = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.selectionStart = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.selectionEnd = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.direction = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.isComposing = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseScrollData(): ScrollData {
  return {
    scrollTop: 0,
    scrollLeft: 0,
    scrollWidth: 0,
    scrollHeight: 0,
    clientWidth: 0,
    clientHeight: 0,
    _unknownFields: {},
  };
}

export const ScrollData: MessageFns<ScrollData> = {
  encode(message: ScrollData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scrollTop !== 0) {
      writer.uint32(9).double(message.scrollTop);
    }
    if (message.scrollLeft !== 0) {
      writer.uint32(17).double(message.scrollLeft);
    }
    if (message.scrollWidth !== 0) {
      writer.uint32(24).sint32(message.scrollWidth);
    }
    if (message.scrollHeight !== 0) {
      writer.uint32(32).sint32(message.scrollHeight);
    }
    if (message.clientWidth !== 0) {
      writer.uint32(40).sint32(message.clientWidth);
    }
    if (message.clientHeight !== 0) {
      writer.uint32(48).sint32(message.clientHeight);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScrollData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseScrollData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.scrollTop = reader.double();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.scrollLeft = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.scrollWidth = reader.sint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.scrollHeight = reader.sint32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.clientWidth = reader.sint32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.clientHeight = reader.sint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseCompositionData(): CompositionData {
  return { data: "", _unknownFields: {} };
}

export const CompositionData: MessageFns<CompositionData> = {
  encode(message: CompositionData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== "") {
      writer.uint32(10).string(message.data);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompositionData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCompositionData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.data = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseAnimationData(): AnimationData {
  return { animationName: "", pseudoElement: "", elapsedTime: 0, _unknownFields: {} };
}

export const AnimationData: MessageFns<AnimationData> = {
  encode(message: AnimationData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.animationName !== "") {
      writer.uint32(10).string(message.animationName);
    }
    if (message.pseudoElement !== "") {
      writer.uint32(18).string(message.pseudoElement);
    }
    if (message.elapsedTime !== 0) {
      writer.uint32(25).double(message.elapsedTime);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnimationData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAnimationData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.animationName = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.pseudoElement = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.elapsedTime = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseTransitionData(): TransitionData {
  return { propertyName: "", pseudoElement: "", elapsedTime: 0, _unknownFields: {} };
}

export const TransitionData: MessageFns<TransitionData> = {
  encode(message: TransitionData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.propertyName !== "") {
      writer.uint32(10).string(message.propertyName);
    }
    if (message.pseudoElement !== "") {
      writer.uint32(18).string(message.pseudoElement);
    }
    if (message.elapsedTime !== 0) {
      writer.uint32(25).double(message.elapsedTime);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransitionData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTransitionData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.propertyName = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.pseudoElement = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.elapsedTime = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseTouchPoint(): TouchPoint {
  return {
    identifier: 0,
    clientX: 0,
    clientY: 0,
    pageX: 0,
    pageY: 0,
    screenX: 0,
    screenY: 0,
    radiusX: 0,
    radiusY: 0,
    rotationAngle: 0,
    force: 0,
    _unknownFields: {},
  };
}

export const TouchPoint: MessageFns<TouchPoint> = {
  encode(message: TouchPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.identifier !== 0) {
      writer.uint32(8).sint32(message.identifier);
    }
    if (message.clientX !== 0) {
      writer.uint32(17).double(message.clientX);
    }
    if (message.clientY !== 0) {
      writer.uint32(25).double(message.clientY);
    }
    if (message.pageX !== 0) {
      writer.uint32(33).double(message.pageX);
    }
    if (message.pageY !== 0) {
      writer.uint32(41).double(message.pageY);
    }
    if (message.screenX !== 0) {
      writer.uint32(49).double(message.screenX);
    }
    if (message.screenY !== 0) {
      writer.uint32(57).double(message.screenY);
    }
    if (message.radiusX !== 0) {
      writer.uint32(65).double(message.radiusX);
    }
    if (message.radiusY !== 0) {
      writer.uint32(73).double(message.radiusY);
    }
    if (message.rotationAngle !== 0) {
      writer.uint32(81).double(message.rotationAngle);
    }
    if (message.force !== 0) {
      writer.uint32(89).double(message.force);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TouchPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTouchPoint();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.identifier = reader.sint32();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.clientX = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.clientY = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.pageX = reader.double();
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.pageY = reader.double();
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }

            message.screenX = reader.double();
            continue;
          }
          case 7: {
            if (tag !== 57) {
              break;
            }

            message.screenY = reader.double();
            continue;
          }
          case 8: {
            if (tag !== 65) {
              break;
            }

            message.radiusX = reader.double();
            continue;
          }
          case 9: {
            if (tag !== 73) {
              break;
            }

            message.radiusY = reader.double();
            continue;
          }
          case 10: {
            if (tag !== 81) {
              break;
            }

            message.rotationAngle = reader.double();
            continue;
          }
          case 11: {
            if (tag !== 89) {
              break;
            }

            message.force = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseTouchData(): TouchData {
  return { touches: [], changedTouches: [], targetTouches: [], modifiers: undefined, _unknownFields: {} };
}

export const TouchData: MessageFns<TouchData> = {
  encode(message: TouchData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.touches) {
      TouchPoint.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.changedTouches) {
      TouchPoint.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.targetTouches) {
      TouchPoint.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.modifiers !== undefined) {
      Modifiers.encode(message.modifiers, writer.uint32(34).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TouchData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTouchData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.touches.push(TouchPoint.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.changedTouches.push(TouchPoint.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.targetTouches.push(TouchPoint.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.modifiers = Modifiers.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseSize(): Size {
  return { width: 0, height: 0, _unknownFields: {} };
}

export const Size: MessageFns<Size> = {
  encode(message: Size, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(9).double(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(17).double(message.height);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Size {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSize();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.width = reader.double();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.height = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseRect(): Rect {
  return { x: 0, y: 0, width: 0, height: 0, _unknownFields: {} };
}

export const Rect: MessageFns<Rect> = {
  encode(message: Rect, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(9).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(17).double(message.y);
    }
    if (message.width !== 0) {
      writer.uint32(25).double(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(33).double(message.height);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Rect {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRect();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.x = reader.double();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.y = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.width = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.height = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseResizeData(): ResizeData {
  return { borderBox: undefined, contentBox: undefined, _unknownFields: {} };
}

export const ResizeData: MessageFns<ResizeData> = {
  encode(message: ResizeData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.borderBox !== undefined) {
      Size.encode(message.borderBox, writer.uint32(10).fork()).join();
    }
    if (message.contentBox !== undefined) {
      Size.encode(message.contentBox, writer.uint32(18).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResizeData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResizeData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.borderBox = Size.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.contentBox = Size.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseVisibleData(): VisibleData {
  return {
    boundingClientRect: undefined,
    intersectionRatio: 0,
    intersectionRect: undefined,
    isIntersecting: false,
    rootBounds: undefined,
    timeMs: 0,
    _unknownFields: {},
  };
}

export const VisibleData: MessageFns<VisibleData> = {
  encode(message: VisibleData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.boundingClientRect !== undefined) {
      Rect.encode(message.boundingClientRect, writer.uint32(10).fork()).join();
    }
    if (message.intersectionRatio !== 0) {
      writer.uint32(17).double(message.intersectionRatio);
    }
    if (message.intersectionRect !== undefined) {
      Rect.encode(message.intersectionRect, writer.uint32(26).fork()).join();
    }
    if (message.isIntersecting !== false) {
      writer.uint32(32).bool(message.isIntersecting);
    }
    if (message.rootBounds !== undefined) {
      Rect.encode(message.rootBounds, writer.uint32(42).fork()).join();
    }
    if (message.timeMs !== 0) {
      writer.uint32(49).double(message.timeMs);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VisibleData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVisibleData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.boundingClientRect = Rect.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.intersectionRatio = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.intersectionRect = Rect.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.isIntersecting = reader.bool();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.rootBounds = Rect.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }

            message.timeMs = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseFrameData(): FrameData {
  return { timeMs: 0, _unknownFields: {} };
}

export const FrameData: MessageFns<FrameData> = {
  encode(message: FrameData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timeMs !== 0) {
      writer.uint32(9).double(message.timeMs);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FrameData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFrameData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.timeMs = reader.double();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseNavigationData(): NavigationData {
  return { href: "", _unknownFields: {} };
}

export const NavigationData: MessageFns<NavigationData> = {
  encode(message: NavigationData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.href !== "") {
      writer.uint32(10).string(message.href);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NavigationData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseNavigationData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.href = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

function createBaseEventPayload(): EventPayload {
  return { family: undefined, textControl: undefined, _unknownFields: {} };
}

export const EventPayload: MessageFns<EventPayload> = {
  encode(message: EventPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.family?.$case) {
      case "mouse":
        MouseData.encode(message.family.value, writer.uint32(10).fork()).join();
        break;
      case "keyboard":
        KeyboardData.encode(message.family.value, writer.uint32(18).fork()).join();
        break;
      case "form":
        FormData.encode(message.family.value, writer.uint32(26).fork()).join();
        break;
      case "pointer":
        PointerData.encode(message.family.value, writer.uint32(34).fork()).join();
        break;
      case "wheel":
        WheelData.encode(message.family.value, writer.uint32(42).fork()).join();
        break;
      case "touch":
        TouchData.encode(message.family.value, writer.uint32(50).fork()).join();
        break;
      case "scroll":
        ScrollData.encode(message.family.value, writer.uint32(58).fork()).join();
        break;
      case "frame":
        FrameData.encode(message.family.value, writer.uint32(66).fork()).join();
        break;
      case "composition":
        CompositionData.encode(message.family.value, writer.uint32(74).fork()).join();
        break;
      case "animation":
        AnimationData.encode(message.family.value, writer.uint32(82).fork()).join();
        break;
      case "transition":
        TransitionData.encode(message.family.value, writer.uint32(90).fork()).join();
        break;
      case "resize":
        ResizeData.encode(message.family.value, writer.uint32(98).fork()).join();
        break;
      case "visible":
        VisibleData.encode(message.family.value, writer.uint32(106).fork()).join();
        break;
      case "navigation":
        NavigationData.encode(message.family.value, writer.uint32(114).fork()).join();
        break;
    }
    if (message.textControl !== undefined) {
      TextControlData.encode(message.textControl, writer.uint32(122).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventPayload();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.family = { $case: "mouse", value: MouseData.decode(reader, reader.uint32()) };
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.family = { $case: "keyboard", value: KeyboardData.decode(reader, reader.uint32()) };
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.family = { $case: "form", value: FormData.decode(reader, reader.uint32()) };
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.family = { $case: "pointer", value: PointerData.decode(reader, reader.uint32()) };
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.family = { $case: "wheel", value: WheelData.decode(reader, reader.uint32()) };
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.family = { $case: "touch", value: TouchData.decode(reader, reader.uint32()) };
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.family = { $case: "scroll", value: ScrollData.decode(reader, reader.uint32()) };
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.family = { $case: "frame", value: FrameData.decode(reader, reader.uint32()) };
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.family = { $case: "composition", value: CompositionData.decode(reader, reader.uint32()) };
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.family = { $case: "animation", value: AnimationData.decode(reader, reader.uint32()) };
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.family = { $case: "transition", value: TransitionData.decode(reader, reader.uint32()) };
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.family = { $case: "resize", value: ResizeData.decode(reader, reader.uint32()) };
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.family = { $case: "visible", value: VisibleData.decode(reader, reader.uint32()) };
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.family = { $case: "navigation", value: NavigationData.decode(reader, reader.uint32()) };
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.textControl = TextControlData.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
