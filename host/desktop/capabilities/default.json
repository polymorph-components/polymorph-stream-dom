{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the single app window: core defaults plus the five app commands this bridge needs (autogenerated as allow-<command> by build.rs's AppManifest::commands).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-spawn-producer",
    "allow-read-chunk",
    "allow-send-event",
    "allow-answer-query",
    "allow-kill-producer"
  ]
}
